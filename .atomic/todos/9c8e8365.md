{
  "id": "9c8e8365",
  "title": "3 样式+逻辑修复",
  "tags": [],
  "status": "completed",
  "created_at": "2026-05-25T11:45:58.789Z"
}

## ✅ 三项全修

### 1. Sidebar footer 固定
- `min-height: 100vh` → `height: 100vh` + `position: sticky; top: 0`
- footer 始终在视口底部，不随页面滚动消失

### 2. 趋势模块居中
- `.explore-section` 加 `display: flex; justify-content: center`
- `.explore-grid` 去掉 `margin: 0 auto`，改用父级 flex 居中

### 3. AI 携带 repo 上下文
- **根因**：`openChat(item)` 同步调用，但 `selectRepo(item)` 是 async，数据未就绪
- **修复**：新增 `handleChatFromTrending()` / `handleChatFromResult()` 先 await fetch 再传 data
- 同步修复 `trending.vue` 中 `onChat` 同样问题
